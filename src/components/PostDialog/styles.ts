import { css } from '@emotion/react';

const postDialogCss = {
  container: css({
    display: 'flex',
    flexDirection: 'column',
    gap: '36px',
    marginTop: '12px',
  }),
};

export default postDialogCss;

