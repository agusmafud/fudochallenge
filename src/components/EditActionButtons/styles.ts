import { css } from '@emotion/react';

const newCommentCss = {
  buttonRow: css({
    display: 'flex',
    gap: '8px',
    alignItems: 'center',
    justifyContent: 'flex-end',
    paddingRight: '8px',
    paddingBottom: '4px',
  }),
};

export default newCommentCss;
